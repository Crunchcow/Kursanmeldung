# Vorlage für die .env-Datei – auf dem Server kopieren und ausfüllen:
# cp .env.example .env
#
# SECRET_KEY: Neuen Schlüssel generieren mit:
#   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

SECRET_KEY=hier-einen-neuen-sicheren-schluessel-eintragen
DEBUG=False
ALLOWED_HOSTS=89.167.0.28,deine-domain.de

# E-Mail (Bestätigungsmail + Storno-Link)
# Im Entwicklungsmodus: console (gibt Mails im Terminal aus, kein SMTP nötig)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# Für Produktion: smtp
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.dein-anbieter.de
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=noreply@westfalia-osterwick.de
# EMAIL_HOST_PASSWORD=dein-passwort
# DEFAULT_FROM_EMAIL=noreply@westfalia-osterwick.de
SITE_URL=http://89.167.0.28
